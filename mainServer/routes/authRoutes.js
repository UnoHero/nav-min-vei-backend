const express = require("express")

//in the "{}" write the different things fron controllers...
/*
const {
    getNmae,

} = require("../controllers/")

const routes = express.Router()

//this get wil get the persons name... so if they are loged inn there name can be showed in the topp right?
router.get("/", getName)

//Make more for the MinVei site... Mail, name and everything needed...
router.get("/")

module.exports = router

*/